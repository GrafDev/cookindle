// Конфигурация приложения Cookindle
export const CONFIG = {
    // Параметры layout
    layout: {
        minWidth: 350,
        maxWidth: 800,
        widthPercent: 90, // процент от viewport width
        heightPercent: 80, // процент от viewport height
        maxHeight: 800,
        padding: 20,
        borderRadius: 10
    },
    
    // Параметры игровой области
    gameArea: {
        borderRadius: 5,
        backgroundColor: 'transparent' // Прозрачный фон
    },
    
    // Параметры PixiJS
    pixi: {
        backgroundColor: 0x000000, // Черный или прозрачный
        backgroundAlpha: 0 // Полностью прозрачный
    },
    
    // Параметры печенья
    cookie: {
        sizePercent: 0.8, // Размер печенья в процентах от минимальной стороны экрана
        painting: "circle", // "circle", "star", "cross"
        patternColor: 0x3C1810, // Цвет узора (темно-коричневый)
        patternCount: 15, // Количество элементов узора
        patternSize: {
            min: 8,
            max: 20
        },
        // Параметры кусочков-масок (как в игре Gemblo)
        pieces: {
            hexGrid: 21, // Количество шестиугольников между противоположными углами описанного шестиугольника
            polygonSides: 9, // Количество углов у многоугольников (6 = шестиугольник, 9 = девятиугольник)
            sizeMultiplier: 1.3, // Множитель размера многоугольников (1.0 = обычный размер, 1.1 = увеличено на 10%)
            randomRotation: true, // Случайный поворот каждого многоугольника
            maxFallingPieces: 91, // Максимальное количество кусочков, которые выпадают при одном клике
            chipAnimation: {
                duration: 1.5, // Длительность падения в секундах
                gravity: 500, // Сила гравитации
                initialVelocity: {
                    x: { min: -50, max: 50 }, // Начальная скорость по X
                    y: { min: -100, max: -50 } // Начальная скорость по Y
                },
                rotation: { min: -3, max: 3 }, // Скорость вращения
                fadeOut: false, // Исчезновение при падении
                scale: { from: 1, to: 0.8 } // Уменьшение размера
            }
        }
    },
    
    // Параметры центральной формы
    centerShape: {
        form: 1, // 1 - круг, 2 - квадрат, 3 - треугольник
        sizePercent: 0.6, // Размер формы в процентах от печенья
        color: 0xEB8D27, // Цвет обводки (оранжевый)
        lineWidth: 25, // Толщина линии
        alpha: 0.0, // Прозрачность (скрыта)
        borderRadius: {
            circle: 0, // Радиус закругления для круга (не применяется)
            square: 0, // Радиус закругления углов для квадрата
            triangle: 12 // Радиус закругления углов для треугольника
        },
        // Параметры второй границы (под основной)
        secondBorder: {
            enabled: false, // Отключить вторую границу
            color: 0xEE9A00, // Цвет из текстуры печенья (оранжевый)
            widthOffset: 5, // Увеличение толщины на 5 пикселей
            alpha: 1.0 // Прозрачность
        },
        // Параметры пульсирующей обводки
        pulse: {
            enabled: true, // Включить пульсацию
            colorFrom: 0xf93e00, // Начальный цвет (золотистый)
            colorTo: 0xf93e00, // Конечный цвет (такой же - без анимации)
            lineWidth: 2, // Толщина пульсирующей линии
            alpha: 0.8, // Общая прозрачность
            speed: 0.4, // Скорость пульсации (циклов в секунду)
            dashed: true, // Пунктирная линия
            dashLength: 2, // Длина штриха
            gapLength: 8 // Длина промежутка
        }
    },
    
    // Параметры иглы
    needle: {
        textureUrl: './src/assets/textures/needle.png',
        mouseOffset: { x: 0, y: 1 }, // Левый нижний угол для мыши
        touchOffset: { x: 0.8, y: 0.2 }, // Смещение вправо-вверх от пальца
        sizePercent: 50, // Размер иглы в процентах от печенья
        visible: true, // Начальная видимость
        // Параметры тени
        shadow: {
            distance: 10, // Расстояние от точки клика (px)
            alpha: 0.5, // Прозрачность тени
            animationDuration: 0.1 // Длительность анимации нажатия в секундах
        },
        // Позиция на мобильных устройствах
        mobile: {
            staticPosition: { x: 0.85, y: 0.5 }, // Позиция справа (в процентах от экрана)
            animationDuration: 0.3 // Время перемещения к касанию
        }
    },
    
    // Параметры системы трещин и отколов
    cracks: {
        // Количество трещин от одного нажатия
        count: {
            min: 2,
            max: 4
        },
        // Длина трещин
        length: {
            min: 50,
            max: 120
        },
        // Толщина линий трещин
        lineWidth: 2,
        // Цвет трещин
        color: 0x8B4513, // Темно-коричневый
        // Прозрачность
        alpha: 0.8,
        // Угол разброса (в радианах)
        angleSpread: Math.PI * 2, // Полный круг
        // Случайность направления
        randomness: 0.3,
        // Скорость анимации появления трещин
        animationSpeed: 0.05,
        // Сегментирование трещин для анимации
        segments: 20
    },
    
    // Параметры осколков
    chips: {
        // Минимальный размер осколка для выпадения
        minSize: 30,
        // Физика падения
        physics: {
            gravity: 0.5,
            initialVelocity: {
                x: { min: -2, max: 2 },
                y: { min: -5, max: -2 }
            },
            rotation: { min: -0.1, max: 0.1 },
            fadeSpeed: 0.02
        },
        // Визуальные эффекты
        visual: {
            scaleReduction: 0.8, // Уменьшение размера при падении
            color: 0xD2691E // Цвет осколков
        }
    },
    
    // Анимации перехода
    transition: {
        fadeOut: {
            duration: 500, // Длительность исчезновения в мс
            easing: 'ease-out'
        },
        fadeIn: {
            duration: 400, // Длительность появления в мс
            easing: 'ease-in',
            delay: 200 // Задержка перед появлением в мс
        }
    },
    
    // Dev настройки
    dev: {
        showBorders: false,
        showLabels: false,
        consoleLogging: true,
        // Принудительная отладка для мобильных (даже в продакшене)
        forceMobileDebug: false,
        // Показ цветных оверлеев (зеленый для краевых центральных, синий для краевых, розовый для центральных)
        showColorOverlays: false,
        // Показ кнопок смены формы (технические кнопки для тестирования)
        showShapeButtons: true,
        // Показ разделенных кусочков с цветовой маркировкой
        showSplitPieces: false,
        splitPieceColors: {
            inner: 0xFF00FF, // Сиреневый для внутренних частей (основание!)
            outer: 0x00FF00, // Зеленый для внешних частей (можно кликать)
            regular: 0x00FF00, // Зеленый для обычных внешних
            centerPiece: 0x8A2B21, //основание!
            edgePiece: 0x0080FF // Синий для крайних
        }
    }
};